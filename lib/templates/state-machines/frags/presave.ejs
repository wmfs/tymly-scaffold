<% if (presaveFn) { %>
      "Next": "PreSave"
    },
    "PreSave": {
      "Type": "Task",
      "Resource": "function:<%= presaveFn %>",
      "InputPath": "$.formData",
      "ResultPath": "$.formData",
<% } %>
